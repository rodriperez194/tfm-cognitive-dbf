[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tfm"
version = "0.1.0"
description = "TFM signal processing package"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]